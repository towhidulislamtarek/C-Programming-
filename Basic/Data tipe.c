#include <stdio.h>
int main()
{
    int x;
    float y;
    double z;
    char t;

    printf("size of int is = %d\n", sizeof(x));
    printf("size of float is = %d\n", sizeof(y));
    printf("size of double is = %d\n", sizeof(z));
    printf("size of chat is = %d\n", sizeof(t));
    return 0;
}
